# Core
Django==5.0.9
djangorestframework==3.15.2
psycopg2-binary==2.9.9
dj-database-url==2.3.0
django-environ==0.11.2
python-dotenv==1.0.1
gunicorn==21.2.0
whitenoise==6.7.0

# Realtime (recording / live transcript)
channels==4.1.0
websockets==15.0.1
asgiref==3.8.1

# AI
openai==1.51.0

# Auth
msal==1.31.1
PyJWT==2.9.0
cryptography==43.0.3

# Document ingestion (ported from v1 — Keep list)
PyMuPDF==1.24.11
python-docx==1.1.2
pytesseract==0.3.13
Pillow==10.4.0
beautifulsoup4==4.12.3

# Web payments (Square — ported from v1)
squareup==37.1.1.20240717

# Utilities
requests==2.32.3
qrcode==8.2
reportlab==4.2.5

# --- Not yet decided, add when settled (see ARCHITECTURE.md open decisions) ---
# Translation vendor: replaces v1's deep-translator/langdetect
# Cloud media storage SDK: depends on chosen cloud host
# app-store-server-library: only if native iOS + StoreKit path is chosen
